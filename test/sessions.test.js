import test from'node:test';import assert from'node:assert/strict';import{ProviderSessionManager}from'../server/provider-sessions.js';
test('rejects unknown providers without opening a browser',async()=>{const manager=new ProviderSessionManager();await assert.rejects(()=>manager.openLogin('unknown'),/Unsupported provider/)});
test('uses Browserless when HOTPLUG_BROWSERLESS_TOKEN is set',()=>{const previous=process.env.HOTPLUG_BROWSERLESS_TOKEN;process.env.HOTPLUG_BROWSERLESS_TOKEN='test-token';try{const manager=new ProviderSessionManager();assert.equal(manager.remote,true);assert.match(manager.wsEndpoint('gemini'),/token=test-token/);assert.match(manager.wsEndpoint('gemini'),/profile=hotplug-gemini/);assert.doesNotMatch(manager.wsEndpoint('gemini',{forLogin:true}),/profile=/);}finally{if(previous===undefined)delete process.env.HOTPLUG_BROWSERLESS_TOKEN;else process.env.HOTPLUG_BROWSERLESS_TOKEN=previous;}});
