import test from 'node:test';import assert from'node:assert/strict';import fs from'node:fs/promises';import os from'node:os';import path from'node:path';import{Store}from'../server/store.js';
test('keys persist as hashes and verify',async()=>{const root=await fs.mkdtemp(path.join(os.tmpdir(),'hotplug-'));const store=new Store(root);const key=await store.createKey('OpenClaw');assert.equal(await store.verifyKey(key.token),true);assert.equal(await store.verifyKey('wrong'),false);const raw=await fs.readFile(path.join(root,'hotplug.json'),'utf8');assert.equal(raw.includes(key.token),false)});
